// lib/studios/creator-lab/hashtag-harvest/limits.ts
// Per-hashtag harvest limits (no global cap across the hashtag list).

export const DEFAULT_MAX_POSTS_PER_HASHTAG = 100;
export const MAX_POSTS_PER_HASHTAG_LIMIT = 100;
