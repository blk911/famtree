# Studios Builder — Known Gaps

1. **AI extraction** — Stub only; no scraping or paid APIs.
2. **Steward approve → invite** — Access requests stored; one-click invite send not built.
3. **Msg Vault thread stub** — Space created; welcome thread not auto-provisioned.
4. **Classic editor** — `/studios/start` remains separate from builder wizard.
5. **Child/guardian** — Copy only; full guardian invite intent routing uses existing AIH flows when steward sends invite manually.
6. **Presentation JSON on Studio row** — Content lives on `StudioBuilderDraft.content`; not denormalized to `Studio` table.
