# ── Dependencies ─────────────────────────────────────────────
node_modules/
.pnp
.pnp.js

# ── Build output ─────────────────────────────────────────────
.next/
out/
dist/
build/

# ── Environment / secrets ─────────────────────────────────────
.env
.env.local
.env.production
.env.development
.env.*.local
# (keep *.example — templates only, no real secrets)

# ── TypeScript ────────────────────────────────────────────────
tsconfig.tsbuildinfo
*.tsbuildinfo

# ── Uploads (local dev only) ──────────────────────────────────
public/uploads/*
!public/uploads/.gitkeep
# Bundled Studios marketing background (referenced by app/studios/layout.tsx)
!public/uploads/STUDIO BKGRND.jpg
# Hero intro — relationship-based business clip (referenced by lib/studios/studioIntroVideo.ts)
!public/uploads/Studios_Relationship-Based_Business_720p_caption.mp4
# Right-column hero clip (referenced by lib/studios/studioIntroVideo.ts)
!public/uploads/studio_3.mp4
# Private Client Feedback — proof card clips
!public/uploads/Testimony 1.mp4
!public/uploads/Testimony 2.mp4
!public/uploads/Testimony 3.mp4
!public/uploads/Testimony 4.mp4
# Studios marketing landing hero (StudiosLandingClient.tsx)
!public/uploads/studio_landing_1.mp4

# ── Logs ─────────────────────────────────────────────────────
*.log
npm-debug.log*

# ── OS / editor ───────────────────────────────────────────────
.DS_Store
Thumbs.db
.vscode/
.idea/

# ── Claude Code session files ─────────────────────────────────
.claude/projects/
.claude/worktrees/
.claude/*.jsonl

# ── Misc ─────────────────────────────────────────────────────
*.zip

.vercel
